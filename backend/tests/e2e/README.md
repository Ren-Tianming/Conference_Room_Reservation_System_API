# End-to-end tests

Place browser or externally deployed API journey tests in this directory. Database and Redis behavior is validated in `tests/integration`; quick isolated API coverage lives in `tests/unit`.
